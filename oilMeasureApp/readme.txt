要求：
1. 采用TCPmodbus，对olimodbus读取一个地址的值；
2. 串口打开后开线程，然后接收nmea0381的GPS信号，解析出经纬度；
3. 将获取的数据经纬度和测量值保存在文件中；
4. 串口的和tcpmodbus的参数可以通过配置文件来打开。

1. 读取oil的值采用了一个C#的modbus库，api写在程序里，具体读几位待修改，库文档见NModbus_net-3.5.chm；
2. 打开串口配置参数后，当串口收到数据后，触发方法解析GPS数据；
3. 保存文件名在程序中，可自己修改，已测试
4. 串口数据即TCP参数均通过配置文件获取，已测试

